#include "../include/Item.hpp"

Item::Item(const std::string& name, int health, int damage, const std::string& rarity, int id)
    : name(name), health(health), damage(damage), rarity(rarity), id(id) {
    // Constructor implementation
}
